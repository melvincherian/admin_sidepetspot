import 'package:flutter/material.dart';

class AccessoryDetail extends StatelessWidget {
  const AccessoryDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(),
    );
  }
}
